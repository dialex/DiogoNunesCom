---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import ProjectCard from "../components/ProjectCard.astro";
import createSlug from "../lib/createSlug";
import { withBase } from "../lib/withBase";

// Custom display order — this list IS the page. Each id is a project filename
// (without `.md`) in src/content/projects/. Reorder by moving lines; a new
// project only shows once added here. The trailing comment is just the date.
const order = [
  "lenda-lenga",
  "dating-guide",
  "fnf-book-companion",
  "pod-ser",
  "testing-course",
  "photography",
  "the-geeky-gecko",
  "jcolor",
  "no-code-website",
  "android-apps",
  "birthday-slack-bot",
  "yumyutton",
  "dcid",
  "google-earth-typewriter",
  "code4pt",
  "open-source",
  "readers-forum",
  "pistae",
  "talks",
  "research",
];

const allProjects = await getCollection("projects");
const projects = order.map((id) => allProjects.find((p) => p.id === id)).filter((p) => p !== undefined);
---

<BaseLayout title="Projects" sideBarActiveItemID="projects">
  <div>
    <h2 class="text-3xl font-bold mb-5">Projects</h2>
  </div>

  {
    projects.map((project, index) => (
      <>
        <ProjectCard
          title={project.data.title}
          img={project.data.image}
          desc={project.data.description}
          date={project.data.date}
          skills={project.data.skills}
          url={withBase(`/projects/${createSlug(project.data.title, project.id)}/`)}
        />
        {index < projects.length - 1 && <div class="divider my-0" />}
      </>
    ))
  }
</BaseLayout>
